Q.4 How to delete one commit or multiple commits permanently Write command for it. 

To delete one commit
command - 
  git reset --hard HEAD~1 (1 specifies the single commit)
  
To delete mutiple commits
command -
  git reset --hard HEAD~3 (3 specifies the number of commit)
  

